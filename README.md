# claude-desktop-springboot-demo
Demo project testing Claude Desktop's backend development capabilities — Spring Boot 3.4 WebFlux REST API with Java 21, H2 in-memory DB, Swagger UI, built and tested entirely through Claude Desktop's embedded browser and terminal.
